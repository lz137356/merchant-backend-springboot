<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
        PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
        "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="db">
    <typeAlias alias="withdrawtype" type="com.lxtx.pay.pojo.WithdrawType"/>
    <select id="WithdrawType.select" resultClass="withdrawtype">
        select * from withdrawtype where Id=#id#
    </select>
    <select id="WithdrawType.getAll" resultClass="withdrawtype">
   	select * from withdrawtype
   </select>
    <insert id="WithdrawType.insert" parameterClass="withdrawtype">
        insert into withdrawtype values (null,#name#,#params#,#status#,#feefix#,#feerate#,#totalfee#,#sort#,#types#,#worktime#,#range#,#createtime#,#currency#)
    </insert>

    <delete id="WithdrawType.deleteByPrimaryKey" parameterClass="int">
        delete from withdrawtype where Id=#id#
    </delete>

    <update id="WithdrawType.update" parameterClass="WithdrawType">
        update withdrawtype p set p.Name=#name#,Params=#params#
                           ,Status=#status#,FeeFix=#feefix#
                           ,FeeRate=#feerate#,Sort=#sort#
                           ,Types=#types#,WorkTime=#worktime#
                           ,`Range`=#range#,Currency=#currency#
        where Id=#id#
    </update>

</sqlMap>
