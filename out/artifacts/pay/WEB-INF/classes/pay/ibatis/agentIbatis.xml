<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
        PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
        "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="db">
    <typeAlias alias="agent" type="com.lxtx.pay.pojo.Agent"/>

    
    <resultMap id="resultmap" class="com.lxtx.pay.pojo.Agent">
        <result property="Id" column="Id"/>
        <result property="UserName" column="UserName"/>
        <result property="AppId" column="AppId"/>
        <result property="Remain" column="Remain"/>
        <result property="Status" column="Status"/>



    </resultMap>
    
    
    
    
    
<select id="Agent.queryOne" parameterClass="java.util.Map" resultClass="com.lxtx.pay.pojo.Agent">
    select * from agent where UserName=#UserName#
</select>


    <select id="Agent.getAll" resultClass="agent" parameterClass="Agent">
        select * from Agent
    </select>

    <select id="Agent.queryAllagent" resultClass="Agent" parameterClass="java.util.Map">
        select  * from  Agent
        <dynamic prepend="where">
            <isNotEmpty prepend="and" property="Status">
                Status =#status#
            </isNotEmpty>
            <isNotEmpty prepend="and" property="userName">
                userName = #userName#
            </isNotEmpty>
        </dynamic>

    </select>
    <select id="Agent.select" resultClass="Agent" parameterClass="int">
        select * from Agent where  Id = #Id#
    </select>
    <insert id="Agent.insert" parameterClass="Agent">
        insert into Agent values
        (null,#userName#,#userPass#,#PayFeeFix#,#PayFeeRate#,#WithdrawFeeFix#,#WithdrawFeeRate#,#status#,#TotalPay#,#TotalPayFee#,#TotalWithdraw#,#TotalWithdrawfee#,#Remain#,#CanOver#)
    </insert>
    <delete id="Agent.deleteByPrimaryKey" parameterClass="int">
        delete from Agent where Id=#Id#
    </delete>
    <update id="Agent.update" parameterClass="Agent">
        update Agent a set
                           a.userName = #userName#,
                           a.UserPass = #userPass#,
                           a.PayFeeFix = #PayFeeFix#,
                           a.PayFeeRate = #PayFeeRate#,
                           a.WithdrawFeeFix = #WithdrawFeeFix#,
                           a.WithdrawFeeRate = #WithdrawFeeRate#,
                           a.Status = #status#
        where  Id = #Id#

    </update>

    
    <select id="Agent.selectWithCpinfo" resultMap="resultmap">
        select a.Id,a.UserName,a.Status,c.AppId,a.Remain from agent a right join cpinfo c on Id=c.AgentId where Id=c.AgentId
    </select>
    
    <update id="Agent.updateStatus" parameterClass="agent">
    update agent set Status=#Status#  where Id=#Id#


    </update>
    <select id="Agent.agentStatistics" resultClass="com.lxtx.pay.pojo.AgentStatistics">
        select
            a.Id,
            a.UserName,
            c.AppId,
            c2.StatDay,
            (select count(*) from paylog p where c2.StatDay=p.CreateDay) as PayCrecount,
            c2.PayAmount,
            (select count(*) from withdrawlog w where c2.StatDay=w.CreateDay) as WdCrecount,
            (select count(*) from withdrawlog w where c2.StatDay=w.WithdrawDay) as WdSuscount,
            c2.WithdrawAmount
        from agent a left join cpinfo c on a.Id=c.AgentId left join cpstat c2 on c.AppId=c2.AppId order by StatDay desc
    </select>
</sqlMap>
